---
phase: 02-clamp-event-lifetime-and-cleanup-derived-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WinFormsApp1/Forms/Form1.Json.cs
  - WinFormsApp1/Forms/Form1.EntryExit.cs
  - WinFormsApp1/Helpers/EventFinalizationHelper.cs
  - WinFormsApp1.Tests/Program.cs
autonomous: true
requirements:
  - EVT-04
  - EVT-05
user_setup: []
must_haves:
  truths:
    - "User can shorten or terminate an event and event boxes disappear immediately after the new ExitFrame."
    - "User can end one event instance without deleting other same-type event instances."
    - "Saved JSON does not include event boxes beyond the finalized ExitFrame."
  artifacts:
    - path: WinFormsApp1/Forms/Form1.Json.cs
      provides: Instance-scoped termination and save-time clamp
      min_lines: 80
      contains: TerminateEventFromCurrentFrame
    - path: WinFormsApp1/Helpers/EventFinalizationHelper.cs
      provides: Shared event lifetime clamp helper
      min_lines: 40
  key_links:
    - from: WinFormsApp1/Forms/Form1.Json.cs
      to: WinFormsApp1/Helpers/EventFinalizationHelper.cs
      via: Shared event instance clamp logic
    - from: WinFormsApp1/Forms/Form1.EntryExit.cs
      to: WinFormsApp1/Helpers/EventFinalizationHelper.cs
      via: Exit-range normalization and trimming
---

<objective>
Make event lifetime trimming immediate and instance-safe.

Purpose: Ensure event boxes vanish after `ExitFrame` in memory, UI, and saved output without harming sibling event instances.
Output: Shared event clamp helper, updated `Q` termination and exit-shortening behavior, plus regression coverage for instance-safe trim.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-clamp-event-lifetime-and-cleanup-derived-artifacts/02-CONTEXT.md
@.planning/phases/02-clamp-event-lifetime-and-cleanup-derived-artifacts/02-RESEARCH.md
@WinFormsApp1/Forms/Form1.Json.cs
@WinFormsApp1/Forms/Form1.EntryExit.cs
@WinFormsApp1/Helpers/EventFinalizationHelper.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize instance-scoped event clamp and trim helpers</name>
  <files>WinFormsApp1/Helpers/EventFinalizationHelper.cs, WinFormsApp1/Forms/Form1.EntryExit.cs</files>
  <read_first>WinFormsApp1/Helpers/EventFinalizationHelper.cs, WinFormsApp1/Forms/Form1.EntryExit.cs, .planning/phases/02-clamp-event-lifetime-and-cleanup-derived-artifacts/02-CONTEXT.md, C:/Users/ANNA/AOLTv1.0/Forms/MainForm.cs</read_first>
  <action>Extend `EventFinalizationHelper` with reusable logic to gather boxes for one `EventInstanceId`, trim boxes beyond a finalized exit frame, and leave same-`EventId` sibling instances untouched. Route event exit-shortening paths through this helper so shrinking an event range immediately removes overflow boxes from memory, following the AOL reference behavior.</action>
  <verify>Build succeeds and code search shows event exit-shortening uses shared instance-scoped trimming logic.</verify>
  <acceptance_criteria>
    - event trim logic is keyed by `EventInstanceId`, not only `EventId`
    - shortening an event range removes boxes after the new exit frame immediately
    - helper behavior leaves different instances of the same event type untouched
  </acceptance_criteria>
  <done>Instance-safe trim logic exists and is reusable across event-lifetime entry points.</done>
</task>

<task type="auto">
  <name>Task 2: Update event termination and save-time clamp behavior</name>
  <files>WinFormsApp1/Forms/Form1.Json.cs, WinFormsApp1.Tests/Program.cs</files>
  <read_first>WinFormsApp1/Forms/Form1.Json.cs, WinFormsApp1.Tests/Program.cs, .planning/phases/02-clamp-event-lifetime-and-cleanup-derived-artifacts/02-RESEARCH.md</read_first>
  <action>Refactor `TerminateEventFromCurrentFrame()` so it targets the selected or current event instance instead of broad `EventId` plus rectangle heuristics. Add regression tests for trimming one instance without touching another, and if save/export still has a path that could emit overflow event boxes, add a final consistency clamp there as a backstop.</action>
  <verify>`dotnet build ETRILabelingTool.sln` succeeds and the test suite passes with new Phase 2 trim cases.</verify>
  <acceptance_criteria>
    - `Q` termination trims only the targeted event instance
    - no event boxes beyond finalized exit remain in exported state
    - regression tests cover sibling same-type events and post-exit trimming
  </acceptance_criteria>
  <done>Event termination and save-time behavior enforce immediate, instance-safe lifetime trimming.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build ETRILabelingTool.sln` succeeds
- [ ] Built test DLL executes successfully
- [ ] Code inspection confirms event trim is instance-scoped and immediate
</verification>

<success_criteria>

- All tasks completed
- EVT-04 is explicitly covered
- EVT-05 cleanup is instance-safe in trim/terminate paths
- No overflow event boxes survive past `ExitFrame` in memory or export
  </success_criteria>

<output>
After completion, create `.planning/phases/02-clamp-event-lifetime-and-cleanup-derived-artifacts/02-01-SUMMARY.md`
</output>
