---
phase: 02-clamp-event-lifetime-and-cleanup-derived-artifacts
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - WinFormsApp1/Forms/Form1.EventPropagation.cs
  - WinFormsApp1/Forms/Form1.Inertia.cs
  - WinFormsApp1/Forms/Form1.Timeline.cs
  - WinFormsApp1.Tests/Program.cs
autonomous: true
requirements:
  - EVT-05
  - TRK-03
user_setup: []
must_haves:
  truths:
    - "Event helper paths do not auto-extend event boxes beyond the finalized exit range."
    - "Event waypoint deletion removes only the targeted event instance and its linked artifacts."
    - "Event segments behave as manual-tracking-first, with cleanup and bounded trim as the only automatic post-finalization behaviors."
  artifacts:
    - path: WinFormsApp1/Forms/Form1.EventPropagation.cs
      provides: Bounded event helper behavior
      min_lines: 100
      contains: PropagateEventBoxWithinRange
    - path: WinFormsApp1/Forms/Form1.Timeline.cs
      provides: Instance-safe event waypoint deletion
      contains: btnDeleteSelectedWaypoint_Click
    - path: WinFormsApp1/Forms/Form1.Inertia.cs
      provides: Event range deletion aligned with exit hard cap
  key_links:
    - from: WinFormsApp1/Forms/Form1.Timeline.cs
      to: WinFormsApp1/Helpers/EventFinalizationHelper.cs
      via: Shared instance-scoped cleanup
    - from: WinFormsApp1/Forms/Form1.EventPropagation.cs
      to: WinFormsApp1/Forms/Form1.Inertia.cs
      via: Event range clamping with no overflow regeneration
---

<objective>
Convert event helper behavior to manual-tracking-first and align delete paths with instance-safe cleanup.

Purpose: Prevent propagation, retracking, and delete flows from recreating or preserving overflow event artifacts after Phase 2 trimming rules are in place.
Output: Bounded event helper paths, instance-safe event deletion, and regression tests for hard-cap behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-clamp-event-lifetime-and-cleanup-derived-artifacts/02-CONTEXT.md
@.planning/phases/02-clamp-event-lifetime-and-cleanup-derived-artifacts/02-RESEARCH.md
@WinFormsApp1/Forms/Form1.EventPropagation.cs
@WinFormsApp1/Forms/Form1.Inertia.cs
@WinFormsApp1/Forms/Form1.Timeline.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disable free-running event auto extension and retracking overflow</name>
  <files>WinFormsApp1/Forms/Form1.EventPropagation.cs, WinFormsApp1/Forms/Form1.Inertia.cs</files>
  <read_first>WinFormsApp1/Forms/Form1.EventPropagation.cs, WinFormsApp1/Forms/Form1.Inertia.cs, C:/Users/ANNA/AOLTv1.0/Forms/MainForm.cs, .planning/phases/02-clamp-event-lifetime-and-cleanup-derived-artifacts/02-CONTEXT.md</read_first>
  <action>Review `PropagateEventBoxWithinRange(...)`, `PropagateEventBoxIfNeeded(...)`, `PropagateEventBoxFromCurrentFrame(...)`, and event-related inertia deletion paths. Preserve only bounded range-fill or cleanup behavior needed to support finalized event segments. Remove or disable automatic event propagation/retracking as a default workflow so event handling becomes manual-tracking-first, with `ExitFrame` as the non-negotiable hard cap.</action>
  <verify>Code inspection confirms event helper paths no longer create overflow event boxes or free-running post-finalization extensions.</verify>
  <acceptance_criteria>
    - event helper paths respect `ExitFrame` as a hard cap
    - no default event auto-extension path remains active after finalization
    - inertia/disappearance cleanup does not reintroduce post-exit event boxes
  </acceptance_criteria>
  <done>Event helper and retracking behavior are bounded to manual-tracking-first rules.</done>
</task>

<task type="auto">
  <name>Task 2: Make event waypoint deletion instance-safe and add regression hooks</name>
  <files>WinFormsApp1/Forms/Form1.Timeline.cs, WinFormsApp1.Tests/Program.cs</files>
  <read_first>WinFormsApp1/Forms/Form1.Timeline.cs, WinFormsApp1.Tests/Program.cs, .planning/phases/02-clamp-event-lifetime-and-cleanup-derived-artifacts/02-RESEARCH.md</read_first>
  <action>Refactor event deletion inside `btnDeleteSelectedWaypoint_Click()` so event cleanup uses `EventInstanceId`-aware matching instead of broad `EventId` range deletion. Add regression coverage or a deterministic test hook proving that deleting one event instance does not remove another same-type event and that no post-exit helper boxes survive after deletion and refresh.</action>
  <verify>`dotnet build ETRILabelingTool.sln` succeeds and the test suite passes with Phase 2 delete/cleanup coverage.</verify>
  <acceptance_criteria>
    - event waypoint deletion is scoped to the targeted event instance
    - same-type sibling events remain after deleting one instance
    - regression coverage exists for deletion plus no-overflow-after-cleanup behavior
  </acceptance_criteria>
  <done>Delete and cleanup consumers honor the same instance-safe lifetime rules as trim/terminate paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build ETRILabelingTool.sln` succeeds
- [ ] Built test DLL executes successfully
- [ ] Code inspection confirms event helper behavior is bounded and deletion is instance-safe
</verification>

<success_criteria>

- All tasks completed
- TRK-03 is covered through hard-cap helper behavior
- Event deletion no longer overreaches across sibling instances
- Phase 2 fully enforces manual-tracking-first event lifetime behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/02-clamp-event-lifetime-and-cleanup-derived-artifacts/02-02-SUMMARY.md`
</output>
