---
phase: 03-stabilize-vehicle-coupling-and-selection-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WinFormsApp1/Logic/TrackingIdentityHelper.cs
  - WinFormsApp1/Forms/Form1.EntryExit.cs
  - WinFormsApp1.Tests/Program.cs
autonomous: true
requirements:
  - TRK-02
user_setup: []
must_haves:
  truths:
    - "One logical vehicle keeps one internal identity across active waypoint and event-assisted flows."
    - "Vehicle body and related helper/plate paths resolve through VehicleInstanceId rather than splitting into multiple visible identities."
  artifacts:
    - path: WinFormsApp1/Logic/TrackingIdentityHelper.cs
      provides: Single vehicle identity source of truth
      min_lines: 40
    - path: WinFormsApp1/Forms/Form1.EntryExit.cs
      provides: Event-assisted vehicle logic aligned with instance identity
      min_lines: 80
  key_links:
    - from: WinFormsApp1/Forms/Form1.EntryExit.cs
      to: WinFormsApp1/Logic/TrackingIdentityHelper.cs
      via: Vehicle identity resolution in entry/exit flows
---

<objective>
Normalize vehicle identity usage around event-assisted waypoint flows.

Purpose: Prevent one logical vehicle from splitting into multiple visible identities during overlap and entry/exit operations.
Output: Event and vehicle flows consistently resolve vehicle identity through `VehicleInstanceId`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-stabilize-vehicle-coupling-and-selection-safety/03-CONTEXT.md
@.planning/phases/03-stabilize-vehicle-coupling-and-selection-safety/03-RESEARCH.md
@WinFormsApp1/Logic/TrackingIdentityHelper.cs
@WinFormsApp1/Forms/Form1.EntryExit.cs
@WinFormsApp1.Tests/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finish the VehicleInstanceId identity pass for active vehicle logic</name>
  <files>WinFormsApp1/Logic/TrackingIdentityHelper.cs, WinFormsApp1/Forms/Form1.EntryExit.cs</files>
  <read_first>WinFormsApp1/Logic/TrackingIdentityHelper.cs, WinFormsApp1/Forms/Form1.EntryExit.cs, .planning/phases/03-stabilize-vehicle-coupling-and-selection-safety/03-CONTEXT.md</read_first>
  <action>Audit vehicle identity usage in event-assisted entry/exit handling and any nearby helper calls. Replace legacy `VehicleId`-based active logic with `TrackingIdentityHelper.GetNumericIdentity(...)` or equivalent `VehicleInstanceId`-based matching where this phase's scope requires it. Preserve event independence while making vehicle-side identity stable.</action>
  <verify>Build succeeds and code inspection shows vehicle identity resolution in active paths uses `TrackingIdentityHelper`/`VehicleInstanceId` rather than mixed `VehicleId` logic.</verify>
  <acceptance_criteria>
    - active vehicle identity resolution is instance-based in the event-assisted paths touched by this phase
    - no new active logic introduces `VehicleId` as the primary internal identity for moving vehicle state
  </acceptance_criteria>
  <done>Vehicle identity is consistently anchored to `VehicleInstanceId` in the targeted active flows.</done>
</task>

<task type="auto">
  <name>Task 2: Extend regression hooks for stable vehicle identity</name>
  <files>WinFormsApp1.Tests/Program.cs</files>
  <read_first>WinFormsApp1.Tests/Program.cs, .planning/phases/03-stabilize-vehicle-coupling-and-selection-safety/03-RESEARCH.md</read_first>
  <action>Add or refine lightweight tests proving that one logical vehicle retains one identity across helper/waypoint matching scenarios relevant to this phase. Reuse the existing Program.cs harness style instead of introducing a new test framework.</action>
  <verify>Built test DLL runs successfully and the new identity regression cases pass.</verify>
  <acceptance_criteria>
    - regression coverage exists for stable vehicle identity behavior
    - tests fail meaningfully if vehicle identity splits regress in the touched paths
  </acceptance_criteria>
  <done>Phase 3 has a regression hook for vehicle identity stability.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build ETRILabelingTool.sln` succeeds
- [ ] Built test DLL executes successfully
- [ ] Code inspection confirms `VehicleInstanceId`-based identity is used in the targeted paths
</verification>

<success_criteria>

- All tasks completed
- TRK-02 is structurally covered
- Vehicle identity no longer drifts across the touched event-assisted flows
  </success_criteria>

<output>
After completion, create `.planning/phases/03-stabilize-vehicle-coupling-and-selection-safety/03-01-SUMMARY.md`
</output>
