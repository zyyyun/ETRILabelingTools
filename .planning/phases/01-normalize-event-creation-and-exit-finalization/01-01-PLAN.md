---
phase: 01-normalize-event-creation-and-exit-finalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WinFormsApp1/Forms/Form1.EntryExit.cs
  - WinFormsApp1/Forms/Form1.Shortcuts.cs
  - WinFormsApp1/Forms/Form1.Drawing.cs
  - WinFormsApp1/Forms/Form1.Helpers.cs
autonomous: true
requirements:
  - EVT-01
  - EVT-02
user_setup: []
must_haves:
  truths:
    - "User can mark Entry without immediately creating an Event Waypoint."
    - "User can finalize an event with the Exit button or X key and both paths create exactly one Event Waypoint."
    - "Vehicle overlap does not change whether the event finalization path creates a waypoint."
  artifacts:
    - path: WinFormsApp1/Forms/Form1.EntryExit.cs
      provides: Canonical event finalization flow
      min_lines: 50
      contains: SetExitMarkerAndCreateWaypoint
    - path: WinFormsApp1/Forms/Form1.Shortcuts.cs
      provides: Keyboard route into the shared finalization path
      contains: SetExitMarkerAndCreateWaypoint
  key_links:
    - from: WinFormsApp1/Forms/Form1.Shortcuts.cs
      to: WinFormsApp1/Forms/Form1.EntryExit.cs
      via: Shared X-key finalization call
      pattern: SetExitMarkerAndCreateWaypoint
---

<objective>
Lock one canonical event finalization authority in the exit flow.

Purpose: Prevent duplicate event waypoint creation by ensuring Entry remains temporary and both exit entry points resolve through the same event-finalization logic.
Output: Updated exit/finalization code path with explicit pending-event semantics and no vehicle-overlap branching for event creation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-normalize-event-creation-and-exit-finalization/01-CONTEXT.md
@.planning/phases/01-normalize-event-creation-and-exit-finalization/01-RESEARCH.md
@WinFormsApp1/Forms/Form1.EntryExit.cs
@WinFormsApp1/Forms/Form1.Shortcuts.cs
@WinFormsApp1/Forms/Form1.Drawing.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define pending-event finalization rules in the shared exit path</name>
  <files>WinFormsApp1/Forms/Form1.EntryExit.cs, WinFormsApp1/Forms/Form1.Helpers.cs</files>
  <read_first>WinFormsApp1/Forms/Form1.EntryExit.cs, .planning/phases/01-normalize-event-creation-and-exit-finalization/01-CONTEXT.md, docs/superpowers/specs/2026-07-21-event-waypoint-stabilization-design.md</read_first>
  <action>Refactor `SetExitMarkerAndCreateWaypoint()` so that event finalization is explicit and event-specific. Keep `Entry` as marker-only state. Add or refine helper logic as needed so event waypoint creation happens exactly once per pending event group, grouped by `EventInstanceId` first. Ensure vehicle overlap is not used as a branch condition for whether an event waypoint is created. Preserve person and vehicle waypoint behavior unless a shared guard is required to support the event flow.</action>
  <verify>Build succeeds after the refactor and code search shows the canonical event creation path lives in `SetExitMarkerAndCreateWaypoint()`.</verify>
  <acceptance_criteria>
    - `Form1.EntryExit.cs` still contains `SetExitMarkerAndCreateWaypoint` as the event finalization authority
    - event finalization logic groups or deduplicates event segments using `EventInstanceId` before waypoint creation
    - no new logic uses vehicle overlap as a prerequisite for creating an event waypoint
  </acceptance_criteria>
  <done>Exit-path code clearly distinguishes temporary entry state from finalized event waypoint creation.</done>
</task>

<task type="auto">
  <name>Task 2: Keep keyboard and draw-time entry points aligned with the canonical finalization model</name>
  <files>WinFormsApp1/Forms/Form1.Shortcuts.cs, WinFormsApp1/Forms/Form1.Drawing.cs</files>
  <read_first>WinFormsApp1/Forms/Form1.Shortcuts.cs, WinFormsApp1/Forms/Form1.Drawing.cs, WinFormsApp1/Forms/Form1.EntryExit.cs</read_first>
  <action>Verify and adjust the `X` shortcut route so it remains a thin call into the shared exit finalization path. Confirm draw-time event box creation only prepares event state (`EventInstanceId`, bbox geometry, pending markers) and does not directly create event waypoint records. If helper comments or guard clauses are misleading, update them so future changes do not reintroduce eager event creation.</action>
  <verify>Search confirms `X` still routes into `SetExitMarkerAndCreateWaypoint()` and draw-time event creation does not directly call `CreateEventWaypoint(...)`.</verify>
  <acceptance_criteria>
    - `Form1.Shortcuts.cs` uses the shared exit finalization path for `X`
    - `Form1.Drawing.cs` creates event boxes and IDs without finalizing a waypoint at draw time
    - comments or guards reflect the new “Entry temporary, Exit finalizes” contract
  </acceptance_criteria>
  <done>All event creation entry points respect the same finalization contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build ETRILabelingTool.sln` succeeds
- [ ] Code inspection confirms `Exit` button and `X` share one event finalization path
- [ ] Code inspection confirms event creation no longer branches on vehicle overlap for waypoint creation
</verification>

<success_criteria>

- All tasks completed
- Event finalization authority is centralized in the exit flow
- EVT-01 and EVT-02 are structurally covered
- No new event creation path bypasses the shared exit finalization logic
  </success_criteria>

<output>
After completion, create `.planning/phases/01-normalize-event-creation-and-exit-finalization/01-01-SUMMARY.md`
</output>
