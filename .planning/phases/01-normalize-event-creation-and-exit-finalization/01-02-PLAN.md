---
phase: 01-normalize-event-creation-and-exit-finalization
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - WinFormsApp1/Forms/Form1.EventPropagation.cs
  - WinFormsApp1/Forms/Form1.EntryExit.cs
  - WinFormsApp1/Forms/Form1.Timeline.cs
  - WinFormsApp1.Tests/Program.cs
autonomous: true
requirements:
  - EVT-01
  - EVT-03
user_setup: []
must_haves:
  truths:
    - "User can draw an event bbox on the exit frame and the tool treats it as the final box for the current event, not a second waypoint."
    - "Event helper paths do not invent extra waypoint records before or after exit finalization."
    - "Maintainer can replay the Phase 1 event-creation scenarios with a checklist tied to button, shortcut, and exit-frame drawing behavior."
  artifacts:
    - path: WinFormsApp1/Forms/Form1.EventPropagation.cs
      provides: Event propagation helpers constrained to finalized ranges
      min_lines: 80
      contains: PropagateEventBoxWithinRange
    - path: WinFormsApp1/Forms/Form1.EntryExit.cs
      provides: Exit-frame bbox final-box handling
      contains: eventBoxesInRange
    - path: WinFormsApp1.Tests/Program.cs
      provides: Placeholder or harness touchpoint for phase-level verification notes if tests are added
  key_links:
    - from: WinFormsApp1/Forms/Form1.EntryExit.cs
      to: WinFormsApp1/Forms/Form1.EventPropagation.cs
      via: Finalized event range passed into propagation helpers
      pattern: PropagateEventBoxWithinRange
---

<objective>
Remove eager event helper side effects and lock exit-frame bbox behavior to the active event segment.

Purpose: Ensure exit-frame drawing cannot create a second event waypoint and helper methods only extend already-finalized event state.
Output: Updated propagation helpers, exit-frame bbox handling, and a concrete regression checklist for Phase 1 behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-normalize-event-creation-and-exit-finalization/01-CONTEXT.md
@.planning/phases/01-normalize-event-creation-and-exit-finalization/01-RESEARCH.md
@WinFormsApp1/Forms/Form1.EntryExit.cs
@WinFormsApp1/Forms/Form1.EventPropagation.cs
@WinFormsApp1/Forms/Form1.Timeline.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip eager waypoint authority from event propagation helpers</name>
  <files>WinFormsApp1/Forms/Form1.EventPropagation.cs, WinFormsApp1/Forms/Form1.EntryExit.cs</files>
  <read_first>WinFormsApp1/Forms/Form1.EventPropagation.cs, WinFormsApp1/Forms/Form1.EntryExit.cs, .planning/phases/01-normalize-event-creation-and-exit-finalization/01-CONTEXT.md</read_first>
  <action>Audit `CreateEventWaypoint(...)`, `PropagateEventBoxToEnd(...)`, `PropagateEventBoxIfNeeded(...)`, and `PropagateEventBoxWithinRange(...)`. Remove or constrain any logic that can create a second event waypoint outside the canonical exit finalization path. Keep propagation helpers usable for copying finalized event boxes within a known range, but ensure they no longer invent event segments on their own.</action>
  <verify>Code inspection shows only finalized-range propagation remains and no helper independently creates a second event waypoint for the same event instance.</verify>
  <acceptance_criteria>
    - propagation helpers no longer act as alternate event-finalization authorities
    - event helper comments and method behavior match the Phase 1 contract
    - any remaining helper-created boxes preserve the existing event instance instead of creating a new segment
  </acceptance_criteria>
  <done>Event helper paths extend finalized state only.</done>
</task>

<task type="auto">
  <name>Task 2: Lock exit-frame bbox semantics and capture a regression checklist</name>
  <files>WinFormsApp1/Forms/Form1.EntryExit.cs, WinFormsApp1/Forms/Form1.Timeline.cs, WinFormsApp1.Tests/Program.cs</files>
  <read_first>WinFormsApp1/Forms/Form1.EntryExit.cs, WinFormsApp1/Forms/Form1.Timeline.cs, docs/superpowers/specs/2026-07-21-event-waypoint-stabilization-design.md</read_first>
  <action>Implement the rule that an event bbox drawn on the exit frame is treated as the last bbox state for the current event only. Prevent it from spawning a second event waypoint or second event candidate. If automated coverage is feasible in the current test harness, add a lightweight regression test or documented test hook; otherwise, update the phase execution notes or summary expectations so the manual regression checklist explicitly covers `Exit button`, `X`, and `exit-frame bbox` scenarios.</action>
  <verify>`dotnet build ETRILabelingTool.sln` succeeds and the changed code path or test artifact makes the exit-frame rule explicit.</verify>
  <acceptance_criteria>
    - exit-frame bbox handling is explicit in code and does not create a second waypoint
    - the phase output includes a concrete regression checklist or test hook for EVT-03
    - timeline/list consumer code is not given new authority to create event segments
  </acceptance_criteria>
  <done>Exit-frame drawing is locked to “final box only” semantics and Phase 1 behavior is verifiable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build ETRILabelingTool.sln` succeeds
- [ ] Code inspection confirms exit-frame bbox is handled as final-box-only
- [ ] A manual or automated regression checklist exists for `Exit`, `X`, and exit-frame drawing
</verification>

<success_criteria>

- All tasks completed
- EVT-03 is explicitly covered
- Event propagation helpers can no longer create parallel event segments
- Phase 1 finishes with a repeatable verification path for the reproduced event-creation cases
  </success_criteria>

<output>
After completion, create `.planning/phases/01-normalize-event-creation-and-exit-finalization/01-02-SUMMARY.md`
</output>
